<%= simple_form_for ([ @exercise_session  ,@exercise_comment ]) do |f| %>
        <%= f.input :comments, prompt: "Leave a comment"%>
        <%= f.button :submit, "Send", class: "btn btn-primary" %>


      <% end %>

      <%= link_to "go back to session", exercise_session_path(@exercise_session), class:"btn btn-primary"%>
<% console %>
